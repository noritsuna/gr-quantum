id: quantum_gates_T
label: Gate T
flags: [ 'python', 'cpp' ]

parameters:
-   id: freq
    label: Frequency
    dtype: real
    default: 1000
-   id: I_amp
    label: I Amplitude
    dtype: float
    default: 1.0
-   id: Q_amp
    label: Q Amplitude
    dtype: float
    default: 1.0
-   id: I_bw
    label: I Bandwidth
    dtype: real
    default: 10
-   id: Q_bw
    label: Q Bandwidth
    dtype: real
    default: 10
-   id: proc_time
    label: Processing Time(ns)
    dtype: real
    default: 1000000
-   id: samples_per_second
    label: Sample Rate
    dtype: real
    default: samp_rate

inputs:
-   domain: message
    id: in

outputs:
-   domain: message
    id: out

templates:
    imports: import quantum
    make: quantum.gates_T(${freq}, ${I_amp}, ${Q_amp}, ${I_bw}, ${Q_bw}, ${proc_time}, ${samples_per_second})

cpp_templates:
    includes: [ '#include <quantum/gates_T.h>' ]
    declarations: 'quantum::gates_T::sptr ${id};'
    make: 'this->${id} = quantum::gates_T::make(${freq}, ${I_amp}, ${Q_amp}, ${I_bw}, ${Q_bw}, ${proc_time}, ${samples_per_second});'
    link: ['gnuradio-quantum']
    translations:
        "'": '"'
        'True': 'true'
        'False': 'false'

documentation: |-
    Please input these parameters into Coprocessor Block, if you want to use same parameters for all gate.

file_format: 1
